/* DELETUS
export const displaySystemMessage = (message) => {
    // Create a new system message element
    const systemMessageElement = document.createElement("div");
    systemMessageElement.classList.add("system-message");
    systemMessageElement.textContent = message;
  
    // Append the system message to the chat interface
    const chatInterface = document.getElementById("chat-interface");
    chatInterface.appendChild(systemMessageElement);
  }


  */ 